// import Image from "next/image";

// type LogoImageProps = {
//   src: string;
// };

// export const LogoImage = ({ src }: LogoImageProps) => {
//   return <Image src={src} alt="公司 Logo" width={120} height={40} />;
// };

export const LogoImage = () => {
  return <div className="">Logo</div>;
};
